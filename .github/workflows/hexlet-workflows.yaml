name: lint-check

# Trigger the workflow on push or pull request
on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - name: Run Linter
        run: |
          make lint
